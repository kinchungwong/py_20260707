# Session retrospective — 2026-07-10

**Theme:** completed **increment 2 of the library promotion — the input layer**
(`InputRouter` + keyboard widget + QWERTY map) into `src/pypiano_2607/`, on top of the
committed increment-1 synth core. (Planning + the increment-1 build are logged in
[2026-07-09-c](2026-07-09-c.md).)

## What got built

- **`src/pypiano_2607/router.py`** — `InputRouter` promoted from `input_integration`
  (reference-count reconciliation unchanged). The one promotion change: `press`/`release`
  now RETURN the 5-field `NoteEvent`, **minting `sounder_id = pitch-slot (MIDI)` and
  `freq = midi_to_freq(midi)`** on the 0→1 / 1→0 transitions (`freq=None` on release).
  This makes the router the library's **pitch→Hz resolution point**. [[sounder-id]]
- **`src/pypiano_2607/gui/keyboard.py`** — the `piano_keyboard` widget, verbatim
  (geometry, black-first `hit_test`, draw, dirty-rect repaint); **pygame stays lazy**.
- **`src/pypiano_2607/gui/qwerty.py`** — the QWERTY→MIDI map + `key_to_midi(pygame)`.
- **38 new pytest tests** (router, keyboard [SDL-dummy], qwerty, integration, gui-package)
  → **106 total, all green**. Includes the now-unblocked event_queue **T3** transit-latency
  test driven through the real router (`slow`), and a subprocess-isolated **pygame-lazy
  guard across all 5 import surfaces**.

## Decisions & why (canonical facts in `../memory/`)

- **`sounder_id` = the pitch slot (MIDI), minted in the router** — forced by preserving
  keyboard+mouse reconciliation (the same note from two sources → one sounding object).
  Cents/JI can slot in here later, upstream of the unchanged synth. [[sounder-id]]
- **Additive-only promotion:** the committed increment-1 files were left byte-identical;
  `InputRouter` lives at `pypiano_2607.router` (top-level API export deferred to the app
  increment). Spikes stay frozen. [[synth-core-library]]
- **pygame stays lazy** (the headless-CI invariant) and the **`set_grab` ban** holds —
  both adversarially verified.

## How it was built

A 6-phase multi-agent workflow (13 agents): build → tests (fan-out) → 6 adversarial
verifiers → integrate → self-heal → critique. Built clean (**0 fix iterations**). One
verifier (`router-mint`) hit a StructuredOutput format cap and returned nothing — that
check was **re-run by hand** (passed). The critic flagged the pygame-lazy guard covering
only `router`; **closed** with a guard over all import surfaces.

## State handoff — where the next session picks up

- **Increments 1–2 are done and green** (`../plans/library-promotion.md`). Increment 1 is
  **committed** (`2ef9c72`); **increment 2 + the memory hubs + the demo + the 2026-07-09-c
  retro are UNCOMMITTED** in the working tree — commit before the app increment to keep
  diffs reviewable.
- **Next: the app increment** — wire it live (PyGame loop → `InputRouter` → `EventQueue` →
  `PolySynth` → sounddevice at `latency='low'`) as an `Instrument`/`App` class, and promote
  the **mouse glissando** drag state machine (deferred out of the input layer).
- **Then microtonal** — genuinely unblocked; cents/JI upstream of the unchanged synth.
- **Ear-test still open:** `examples/play_synth.py` (headless-verified; DSP bit-exact to the
  already-approved `piano_voice` spike, so confirmation not risk).

## What went well / what to watch

- **Additive, frozen-increment discipline worked:** increment 2 touched zero increment-1
  files and zero spikes; both test nets (pytest + `run_spike_tests.py`) stayed green.
- **Watch:** an agent's StructuredOutput can occasionally fail to format under the retry
  cap — cross-check any workflow verdict that goes missing (as done here). And the working
  tree is carrying a lot uncommitted.
