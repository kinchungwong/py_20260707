# Session retrospective — 2026-07-11 (first focused spike: staged-chord-entry)

Planned the PyGame side of staged chord entry, then scaffolded and verified the first
focused spike. **What was built and every decision behind it is in the spike's own docs —
not repeated here.** This entry is the hand-off.

## Read these instead of a recap

- [`focused-spikes/staged-chord-entry/README.md`](../../focused-spikes/staged-chord-entry/README.md)
  — the question, controls, how to run / `--selftest`, the pinned "spike-simple" decisions,
  and the (empty) `## Finding`.
- [`focused-spikes/staged-chord-entry/status_active.md`](../../focused-spikes/staged-chord-entry/status_active.md)
  — the named debt (audition → `PolySynth` at graduation).
- [`../tasks/current.md`](../tasks/current.md) — the pinned minimal slice and the src-path
  friction resolution.

## Hand-off — what remains

**1. The point of the spike is not yet done: the feel test.** Everything so far is
mechanics. The actual question — *does staged entry feel like playing, or like
programming?* — needs a human at real hardware (`.venv/bin/python
focused-spikes/staged-chord-entry/main.py`; the spike verified only headlessly here). Fill
the spike README's `## Finding`. Everything below is contingent on that verdict.

**2. Commit.** The spike + the `shared/` trunk + doc edits sit in the working tree,
uncommitted (spike files staged mid-session; `shared/*.py` untracked). `git add -A` then
commit captures the lot as one landing.

**3. If the verdict is "keep going," next round's agenda** (all deliberately outside this
slice):
- **Commit-gesture comparison.** Only the sketch's toggle + explicit-commit shape was built.
  If it reads as "programming," the untried alternatives are the quasimode (hold-modifier,
  release-fires) and the timeout/auto-commit — see [retro-e's gap list](2026-07-10-e.md) and
  [keyboard-and-input-surface](../discussions/2026-07-10/keyboard-and-input-surface.md).
- **Audition consolidation** into `PolySynth` (the status-marker debt) — removes the second
  acoustic authority.
- **Tuning fidelity.** Auditions are 12-TET; committed notes honour the real tuning. Under
  JI, whether staged/saved notes are tuning-relative and re-resolve on a tonic change is
  unaddressed.
- **Parked interaction gaps** still open from [retro-e](2026-07-10-e.md): preset persistence
  across restarts (session-only now), mouse-in-staged (keyboard-first v1; mouse drives only
  HUD buttons), keyboard bindings for Save/Release (mouse-only), Reset (omitted).

**4. Watch — the `focused-spikes/` policy is now only half-tested.** The active tier and the
`shared/` trunk got exercised for real: the library-import friction surfaced and resolved
into [`shared/bootstrap.py`](../../focused-spikes/shared/README.md) (READMEs updated to
match). Still untested is the **archived / hardening tier** — inlining `shared/` and
vendoring the library into an extractable spike. The first archival will be its own
contact-with-reality moment; treat that part of the policy as provisional too.
